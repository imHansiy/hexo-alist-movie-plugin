style.
  :root {
    --bg-color: #ffffff;
    --text-color: #1f2937;
    --border-color: #e5e7eb;
    --header-bg: #f9fafb;
    --row-hover-bg: #f3f4f6;
    --success-color: #10b981;
    --fail-color: #ef4444;
    --link-color: #3b82f6;
  }
  @media (prefers-color-scheme: dark) {
    :root {
      --bg-color: #111827;
      --text-color: #d1d5db;
      --border-color: #374151;
      --header-bg: #1f2937;
      --row-hover-bg: #374151;
    }
  }
  .comparison-content {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    line-height: 1.6;
    color: var(--text-color);
    padding: 2rem 0;
  }
  .comparison-content h1 {
    text-align: center;
    font-size: 2.25rem;
    margin-bottom: 2rem;
    font-weight: 800;
  }
  .table-container {
    overflow-x: auto;
    border: 1px solid var(--border-color);
    border-radius: 12px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }
  th, td {
    padding: 12px 16px;
    text-align: left;
    border-bottom: 1px solid var(--border-color);
  }
  th {
    background-color: var(--header-bg);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  tbody tr:last-child td {
    border-bottom: none;
  }
  tbody tr:hover {
    background-color: var(--row-hover-bg);
  }
  .poster {
    width: 50px;
    height: 75px;
    object-fit: cover;
    border-radius: 6px;
    vertical-align: middle;
  }
  .movie-info {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .movie-title {
    font-weight: 600;
  }
  .original-filename {
    word-break: break-all;
    font-family: monospace;
    font-size: 0.85rem;
  }
  .status-badge {
    display: inline-block;
    padding: 4px 10px;
    border-radius: 999px;
    font-weight: 600;
    font-size: 0.8rem;
    text-transform: capitalize;
  }
  .status-badge.success {
    background-color: rgba(16, 185, 129, 0.1);
    color: var(--success-color);
  }
  .status-badge.fail {
    background-color: rgba(239, 68, 68, 0.1);
    color: var(--fail-color);
  }
  .no-data {
    text-align: center;
    padding: 3rem;
    font-size: 1.1rem;
    color: #6b7280;
  }

.comparison-content
  h1 文件识别对比

  if movies && movies.length > 0
    .table-container
      table
        thead
          tr
            th(style="width: 35%;") 识别结果
            th(style="width: 55%;") 原始文件名
            th(style="width: 10%;") 状态
        tbody
          each movie in movies
            each file in movie.files
              tr
                td
                  .movie-info
                    if movie.poster_path
                      img.poster(src=`https://image.tmdb.org/t/p/w200${movie.poster_path}`, alt=movie.title)
                    span.movie-title= movie.title
                td
                  .original-filename= file.name
                td
                  if movie.media_type !== 'unknown'
                    span.status-badge.success 成功
                  else
                    span.status-badge.fail 失败
  else
    .no-data
      p 没有找到任何电影数据。